*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
app/release/
app/debug/
*.apk
*.aab
*.keystore
*.jks
# Clave de depuración compartida (no es secreta, es solo para builds de
# debug de CI): hace falta fija para que las actualizaciones no pidan
# desinstalar la app cada vez que cambia de máquina de compilación.
!keystore/debug.keystore
